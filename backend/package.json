{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "Mock REST API (json-server) that serves RD Station products.",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
